/**
 * @deprecated Import from `@shared` (`shared/index.ts`) or the owning
 * service folder (`@shared/users/types`, `@shared/health/types`) instead.
 * Kept for backwards compatibility — re-exports the composed contract.
 */
export * from './index'
